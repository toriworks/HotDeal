<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!--설정화면에서 사용하는 POPUP MENU의 메뉴 아이템 -->

    <!-- 로그아웃 -->
    <item
            android:id="@+id/menu_settings_logout"
            android:title="로그아웃"
            android:orderInCategory="0" />
    <!-- 이용약관 -->
    <item
            android:id="@+id/menu_settings_contract"
            android:title="이용약관"
            android:orderInCategory="1" />
    <!-- 개인정보보호정책 -->
    <item
            android:id="@+id/menu_settings_act"
            android:title="개인정보보호정책"
            android:orderInCategory="2" />
    <!-- 탈퇴신청 -->
    <item
            android:id="@+id/menu_settings_out"
            android:title="탈퇴신청"
            android:orderInCategory="3" />
</menu>